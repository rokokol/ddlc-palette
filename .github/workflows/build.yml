name: build

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:
  # Called by update-lock.yml against its temp branch, so the lock bump is checked by this
  # file rather than by a copy of its commands
  workflow_call:
    inputs:
      ref:
        type: string
        required: false
        default: ""

permissions:
  contents: read

jobs:
  nix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref }}
      - uses: cachix/install-nix-action@v31

      # The versioning skill's checker, vendored: VERSION against CHANGELOG, and the
      # changelog's shape, from one file rather than an inline grep that checked half of it
      - name: VERSION matches CHANGELOG
        run: ./check-changelog.sh CHANGELOG.md

      - run: nix build --no-write-lock-file
      # dist-is-current, palette-is-annotated, base16-is-sane, scripts-lint
      - run: nix flake check --no-write-lock-file
      # The colours are consumed as lib, not as files, so the attrset itself is the artifact
      - name: the palette evaluates
        run: nix eval --json .#lib.palette
      - run: nix fmt -- --ci

  # The one file list lives in the flake's scripts-lint check — this job is a fast,
  # separately-named status for it, not a second copy of the commands
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref }}
      - uses: cachix/install-nix-action@v31
      # nix run/shell nixpkgs#tool is a mirror-fate lookup — whatever nixpkgs the runner
      # resolves today decides the job. Tools a workflow needs live in the flake's dev
      # shell, pinned by the lock, reached with nix develop
      # The pin guard below is vendored: a copy edited here instead of at its source fails
      # this step by name, before it can run as anything but the source's own text
      - name: vendored copies are byte-equal to their source
        run: ./vendor-sync.sh check
      - name: CI tools come from the lock, not the registry
        run: ./check-pins.sh
      - run: nix build --no-write-lock-file .#checks.x86_64-linux.scripts-lint
